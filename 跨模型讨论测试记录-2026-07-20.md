# 跨模型鞍钢宪法讨论测试记录
## 日期：2026-07-20
## 测试题目：AT32F407 4通道ADC温度采集，12位精度，100Hz
## 参与角色：TL(GLM) + SE(GLM) + QA(GLM) + 外部审查员(Codex/OpenAI)

---

## 第1轮：发散

### 1.1 Tech Lead（GLM模型）— 问题拆解

【角色】Tech Lead（干部）
【置信度】L2（AT32F407芯片特定参数标L3待查数据手册）

拆解为4个子问题：
1. 硬件与通道映射（P1）：ADC单元、通道、GPIO、传感器类型、参考电压
2. 采样率实现机制（P2）：定时器触发 vs 软件轮询
3. 12位精度保障（P3）：采样时间、校准、滤波、过采样
4. 数据流架构（P4）：DMA/中断、温度换算、RTOS、输出

关键歧义：100Hz是每通道100Hz还是4通道共100Hz？

### 1.2 Staff Engineer（GLM模型）— 候选方案

【角色】Staff Engineer（技术人员）
【置信度】整体L2，AT32F407特定参数L3

- 子问题1：推荐NTC分压+ADC1规则组4通道扫描
- 子问题2：推荐定时器TRGO触发+DMA（100Hz扫描4通道）
- 子问题3：推荐长采样时间+校准+均值滤波(8点)+中值滤波
- 子问题4：推荐DMA循环模式+半/全中断双缓冲+裸机主循环

### 1.3 QA Engineer（GLM模型）— 否决

【角色】QA Engineer（工人，有否决权）
【审查结果】**否决**

致命质疑：
1. NTC分压无法达到12位有效精度（ENOB通常仅8-10位）— 根本性矛盾
2. 采样率逻辑自相矛盾（100Hz扫描4通道=每通道100Hz，非25Hz）
3. AT32F407 DMA架构≠STM32F407，可能无硬件双缓冲（DBM）
4. DMA竞态分析不完整

### 1.4 外部审查员（Codex/OpenAI模型）— 独立审查

【角色】外部审查员（跨模型独立视角）
【置信度】见各条标注

**独立验证结果（websearch交叉验证）：**

1. **QA质疑3完全正确** ✅ L1已验证
   - AT32F407数据手册（V2.01/V2.04）明确写"14-channel DMA controller"
   - STM32F407是"16-stream DMA controller with FIFOs and burst support"+DBM位
   - AT32F407的DMA架构类似STM32F1（通道式），不是STM32F4（流式）
   - **结论：AT32F407确实没有硬件DMA双缓冲模式，SE的"DMA双缓冲"措辞误导**

2. **QA质疑1基本正确，但需补充** L2
   - NTC分压ENOB确实达不到12位，QA的判断正确
   - 补充：如果用户说的"12位精度"是指ADC分辨率（标称值），而非有效精度（ENOB），则NTC方案可行
   - 关键：需要用户澄清"12位精度"的含义
   - AT32F407 ADC确实是12位分辨率（L1，数据手册确认）

3. **QA质疑2正确** L1
   - 100Hz触发4通道扫描 = 每通道100Hz，不是25Hz
   - SE的"抽稀"逻辑确实不通

4. **GLM模型整体未发现的问题（Codex补充）**：
   - AT32F407最高主频240MHz（非STM32F407的168MHz），ADC时钟配置不同 L2
   - AT32F407的ADC采样时间选项需要查reference manual确认（AT32F435/437是2.5-640.5周期，但F407可能不同）L3
   - AT32F407有内部温度传感器（数据手册提到"ADC sampling time when reading the temperature: 8.6-17.1μs"），如果只是测芯片温度可以用内部传感器 L2

**外部审查员结论：同意QA否决。SE需修正后重审。**

---

## 第1轮摘要

- TL拆解了4个子问题，关键歧义：100Hz含义
- SE提出4组候选方案，推荐NTC+定时器触发+DMA+均值滤波
- QA否决：NTC精度矛盾、采样率逻辑错误、AT32 DMA无DBM
- 外部审查员（Codex）独立验证：确认QA的AT32 DMA质疑正确（14-channel vs 16-stream），补充AT32F407主频/ADC时钟差异
- **四方一致：SE需修正后进入第2轮**

---

## 跨模型制衡效果评估

| 发现 | GLM模型（TL/SE/QA） | Codex（外部审查员） | 跨模型价值 |
|------|---------------------|---------------------|-----------|
| NTC精度矛盾 | QA发现 ✅ | 确认+补充 | 互补 |
| 采样率逻辑错误 | QA发现 ✅ | 确认 | 一致 |
| AT32 DMA无DBM | QA怀疑(L3) | websearch确认(L1) | **Codex将L3提升为L1** |
| AT32主频240MHz差异 | 未发现 ❌ | Codex补充 | **跨模型独有发现** |
| 内部温度传感器选项 | 未发现 ❌ | Codex补充 | **跨模型独有发现** |

**结论：跨模型制衡有效。Codex发现了GLM模型未注意到的2个问题，并将QA的L3质疑验证为L1。**
