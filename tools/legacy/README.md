# 旧工具

这里保存仅供复现历史操作的辅助脚本，不是当前主产品入口。

- `run_discussion.py`：以固定历史议题调用根目录 `ansteel_agents.py`。
- `patch_v33.py`：对旧 Python 原型应用一次性 V3.3 补丁；运行前应先审查其修改内容。
- `write_plan.py`：重新生成历史 Cline 改造方案到 `docs/legacy/design/`。

当前主产品开发请使用 [`pi-agent/`](../../pi-agent/) 及其文档和测试。
