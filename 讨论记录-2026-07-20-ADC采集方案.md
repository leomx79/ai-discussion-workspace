# 嵌入式专用：鞍钢宪法循环讨论 — ADC采集方案设计
# 日期：2026-07-20
# 目标芯片：AT32F407（雅特力，Cortex-M4，STM32兼容）
# 用户原始输入："我要做一个ADC采集，采集温度传感器信号，
#               不知道用SPI还是DMA，滤波怎么做，对系统有什么影响。"

---

## 第1轮：发散

### 步骤1.1 — TL拆解问题 + 读取项目上下文

**【角色】Tech Lead（干部）**
**【置信度】L1 🟢 已验证（芯片参数来自AT32F407数据手册）**

**立项理由**：用户需要在AT32F407上实现温度传感器ADC采集，
但不确定通信方式（SPI/DMA/中断）、信号调理、滤波算法、系统影响。

**芯片能力确认（干部参加劳动，查手册）：**

AT32F407 ADC外设参数：
| 参数 | 值 | 来源 |
|---|---|---|
| ADC数量 | 3个（ADC1/2/3） | L1🟢 数据手册 |
| 分辨率 | 12位 | L1🟢 |
| 采样率 | 最高1MSPS（单通道） | L1🟢 |
| 输入通道 | 最多16个外部+2个内部 | L1🟢 |
| 输入范围 | 0 ~ VREF+（通常0~3.3V） | L1🟢 |
| DMA支持 | ✅ 每个ADC有独立DMA通道 | L1🟢 |
| 采样时间 | 可配置（1.5~239.5个ADC时钟周期） | L1🟢 |
| 对齐方式 | 左对齐/右对齐 | L1🟢 |

**拆解为6个子问题：**
1. **传感器接口**：温度传感器输出什么信号？（模拟电压？数字接口？）
   → 决定用ADC还是SPI/I2C直接读
2. **ADC采集方式**：轮询？中断？DMA？
   → 取决于采样率需求和CPU占用
3. **模拟前端**：需不需要信号调理？（运放？分压？抗混叠滤波？）
4. **数字滤波**：采集后怎么处理？（均值？IIR？中值？）
5. **系统影响**：对中断、DMA、功耗、其他外设有何影响？
6. **代码实现**：具体寄存器配置和代码

**讨论边界**：
- ✅ 讨论：ADC采集链路完整设计（传感器→调理→ADC→滤波→应用）
- ❌ 不讨论：PCB布局布线（需要硬件工程师）、传感器选型（用户已确定）

**优先级**：先①②（架构决策），再③④（信号处理），最后⑤⑥（实现）

---

### 步骤1.2 — SE提出候选方案

**【角色】Staff Engineer（技术人员）**

**子问题①：传感器接口判断**

温度传感器常见类型：
| 类型 | 输出 | 接口 | 举例 | 置信度 |
|---|---|---|---|---|
| 模拟输出 | 电压（如10mV/°C） | → ADC | LM35、TMP36、热电偶+运放 | L2🟡 |
| 数字输出 | SPI/I2C | → SPI/I2C | DS18B20(1-Wire)、SHT30(I2C)、MAX31855(SPI) | L2🟡 |
| 电阻型 | 电阻变化 | → ADC（需激励电路） | NTC热敏电阻、PT100 | L2🟡 |

**关键判断**：
- 如果传感器是**模拟输出**（LM35、NTC）→ 用ADC
- 如果传感器是**数字输出**（DS18B20、SHT30）→ 用SPI/I2C，不需要ADC
- 用户说"做ADC采集"→ 大概率是模拟输出传感器
- 置信度：**L3 🟠 待确认**（需要用户确认传感器型号）

**以下按"模拟输出温度传感器"设计（如LM35或NTC）。**

**子问题②：ADC采集方式**

| 方案 | 原理 | CPU占用 | 适用场景 | 置信度 |
|---|---|---|---|---|
| A：轮询（Polling） | 主循环里读ADC | 高（CPU等待） | 单次采集、低速 | L2🟡 |
| B：中断（Interrupt） | ADC转换完成触发中断 | 中（每次进中断） | 中速、多通道 | L2🟡 |
| C：DMA | ADC自动搬运到内存 | **极低**（CPU不参与） | 高速、连续采集 | L2🟡 |
| D：DMA+中断 | DMA搬完一批后中断通知 | 低（批量处理） | **推荐：高速+低CPU** | L2🟡 |

**推荐方案D（DMA+中断）**，因为：
- 温度采集虽然不需要1MSPS，但DMA让CPU完全解放
- DMA搬完N个采样后触发中断，CPU一次性处理一批数据
- 不选A：轮询浪费CPU，主循环被阻塞
- 不选B：每次转换都进中断，频繁打断主程序
- 不选C（纯DMA）：没有中断通知，CPU不知道数据何时就绪
- 置信度：**L2 🟡**（基于嵌入式通用设计原则）

**SPI的问题**：
- 用户问"用SPI还是DMA"——**这不是二选一的关系！**
- SPI是**通信协议**（ADC芯片与MCU之间的数据传输）
- DMA是**数据搬运方式**（MCU内部外设到内存）
- 如果用的是**MCU内部ADC**（AT32F407自带）→ 不需要SPI，用DMA
- 如果用的是**外部ADC芯片**（如ADS1115、MCP3008）→ 需要SPI/I2C，可以SPI+DMA
- 置信度：**L1 🟢**（SPI和DMA是不同层面的概念）

**子问题③：模拟前端（信号调理）**

以LM35为例（输出10mV/°C，0°C=0V，100°C=1V）：

| 问题 | 分析 | 方案 | 置信度 |
|---|---|---|---|
| 信号幅度 | LM35输出0~1V，ADC范围0~3.3V | 可以直连，但只用30%量程 | L2🟡 |
| 提高精度 | 放大到0~3.3V → 用满量程 | 运放放大3.3倍（如OPA333） | L2🟡 |
| 抗混叠 | ADC采样前必须滤除高频噪声 | RC低通滤波（截止频率=采样率/2以下） | L1🟢 |
| 参考电压 | VREF+的稳定性直接影响精度 | 用独立基准（如REF3033）而非VCC | L2🟡 |
| NTC情况 | NTC是非线性电阻 | 需要分压电路+查表/Steinhart-Hart公式 | L2🟡 |

**推荐**：
- 如果精度要求不高（±1°C）→ LM35直连ADC + RC滤波
- 如果精度要求高（±0.1°C）→ 运放放大 + 独立基准 + 有源滤波
- 置信度：**L2 🟡**

**子问题④：数字滤波**

| 方案 | 原理 | 优点 | 缺点 | 适用 | 置信度 |
|---|---|---|---|---|---|
| 算术均值 | N次采样取平均 | 简单、有效 | 延迟大、内存占用 | 稳态信号 | L2🟡 |
| 滑动均值 | 滑动窗口平均 | 实时性好 | 内存占用 | 缓变信号 | L2🟡 |
| 中值滤波 | N次取中值 | 抗脉冲干扰 | 计算量大 | 有尖峰噪声 | L2🟡 |
| 一阶IIR | y=a*x+(1-a)*y_prev | 内存极小（1个变量） | 相位延迟 | **嵌入式首选** | L2🟡 |
| 中值+均值 | 先中值去尖峰，再均值平滑 | 综合效果好 | 代码稍复杂 | **推荐组合** | L2🟡 |

**推荐：中值滤波（去尖峰）+ 一阶IIR（平滑）**
- 先取5次中值，去掉脉冲干扰
- 再一阶IIR：`y = 0.1*x + 0.9*y_prev`（a=0.1，平滑系数）
- 内存占用：5个采样缓冲 + 1个滤波状态 = 极少
- 不选纯均值：温度是缓变信号，IIR更适合
- 置信度：**L2 🟡**

**子问题⑤：系统影响**

| 影响项 | 分析 | 对策 | 置信度 |
|---|---|---|---|
| DMA通道冲突 | AT32F407的DMA1有7个通道，ADC1用通道1 | 检查其他外设是否占用同一通道 | L2🟡 |
| 中断优先级 | ADC DMA完成中断不能太高，否则影响通信 | 设为中等优先级（如5） | L2🟡 |
| 功耗 | 连续ADC采集增加功耗 | 不需要时关闭ADC时钟 | L2🟡 |
| 采样率vs精度 | 采样越快，精度越低（充电时间不够） | 温度采集10-100Hz足够，采样时间设长 | L2🟡 |
| GPIO复用 | ADC引脚不能同时做GPIO | 配置为模拟输入模式 | L1🟢 |
| 时钟配置 | ADC时钟不能超过14MHz（AT32） | 从APB2分频 | L2🟡 |

---

### 步骤1.3 — QA挑战

**【角色】QA Engineer（工人，有否决权）**
**【审查模式】黑盒审查**

**质疑1：传感器类型未确认**
- 定位：SE说"大概率是模拟输出传感器" [L3🟠]
- 问题：**这是整个方案的基础！** 如果传感器是DS18B20（数字输出），
  整个ADC方案都不需要，直接1-Wire读取。
- 风险：方案可能完全错误。
- 建议：**必须向用户确认传感器型号。** 不能假设。

**质疑2：SPI和DMA的关系**
- 定位：SE说"SPI和DMA不是二选一"
- 评价：✅ 这个澄清非常好。很多新手确实混淆这两个概念。
- 补充：但如果用户用的是外部SPI ADC（如MCP3008），
  那SPI+DMA的组合方案也需要给出。SE只给了内部ADC方案。
- 建议：补充外部SPI ADC的方案（至少提一下）。

**质疑3：采样率计算缺失**
- 定位：SE说"温度采集10-100Hz足够"
- 问题：**没有计算过程！** 为什么10-100Hz？
  温度变化的时间常数是多少？根据采样定理，采样率至少是信号频率的2倍。
  温度变化通常<1Hz，所以10Hz确实够，但**必须给出推理过程**。
- 建议：补充采样率计算：温度变化<1Hz → 采样率≥2Hz → 实际取10-50Hz（留余量）

**质疑4：抗混叠滤波参数缺失**
- 定位：SE说"RC低通滤波"
- 问题：RC的R和C取多少？截止频率是多少？
  如果采样率50Hz，截止频率应<25Hz（奈奎斯特）。
  但RC值还受信号源阻抗影响。
- 建议：给出具体RC值。例如：R=1kΩ, C=10μF → fc=1/(2πRC)≈16Hz

**质疑5：AT32 vs STM32的差异**
- 定位：SE的方案基于"STM32兼容"假设
- 问题：AT32F407虽然兼容STM32，但**寄存器名称和HAL库不同！**
  AT32用的是Artery自己的HAL库（AT32F4xx_HAL），不是STM32 HAL。
  代码不能直接复制STM32的代码。
- 风险：用户按STM32代码写，编译不过。
- 建议：代码必须用AT32 HAL库的API，或明确标注"以AT32 HAL为例"。

**质疑6：遗漏**
- ❌ 没有考虑ADC校准（AT32的ADC需要上电校准）
- ❌ 没有考虑多通道采集时的通道切换时间
- ❌ 没有考虑VREF+的去耦电容
- ❌ 没有给出完整的信号链路框图

**【否决】❌**
理由：
1. 传感器类型未确认（质疑1）——方案基础不牢
2. 采样率和滤波参数没有计算过程（质疑3、4）
3. AT32 HAL库差异未考虑（质疑5）
4. ADC校准等关键步骤遗漏（质疑6）

---

## 第2轮：收敛

### 步骤2.1 — SE修正+深化

**【角色】Staff Engineer（技术人员）**

**回应质疑1（传感器类型）：**
- 你说得对，这是方案基础。
- 修正：方案分两条路径：
  - **路径A：模拟传感器**（LM35/TMP36/NTC）→ ADC采集（本方案重点）
  - **路径B：数字传感器**（DS18B20/SHT30）→ 1-Wire/I2C/SPI直读（不需要ADC）
- 请用户确认传感器型号后选择路径。
- 以下深化按**路径A（模拟传感器，以LM35为例）**。

**回应质疑2（外部SPI ADC）：**
- 补充：如果使用外部SPI ADC（如MCP3008，10位8通道）：
  - 接口：SPI（AT32的SPI1/2/3）
  - 可以用SPI+DMA：DMA自动发送/接收SPI数据
  - 适用场景：需要更多通道或更高分辨率
  - 但温度采集通常不需要，内部ADC够用
- 置信度：L2 🟡

**回应质疑3（采样率计算）：**
- 修正，补充计算过程：
  - 温度信号变化频率：通常<0.1Hz（室温变化很慢）
  - 奈奎斯特定理：采样率 ≥ 2 × 0.1Hz = 0.2Hz
  - 实际取10-50Hz（留100倍以上余量，抗干扰）
  - ADC转换时间：12位，采样时间239.5周期 → 约20μs/次
  - 50Hz采样 → 每20ms采一次 → CPU占用<0.1%（DMA模式下）
- 置信度：L2 🟡

**回应质疑4（RC滤波参数）：**
- 修正，给出具体值：
  - 采样率50Hz → 奈奎斯特频率25Hz
  - 抗混叠截止频率：取10Hz（留余量）
  - RC计算：fc = 1/(2πRC) = 10Hz
  - 取 R=1.5kΩ, C=10μF → fc = 1/(2π×1500×10e-6) ≈ 10.6Hz ✅
  - 注意：R不能太大（增加噪声），C不能太小（滤波效果差）
  - LM35输出阻抗<1Ω，所以R=1.5kΩ不影响信号
- 置信度：L2 🟡

**回应质疑5（AT32 HAL库）：**
- 你说得对！AT32和STM32的HAL库API不同。
- 修正：代码使用AT32F4xx HAL库API。
  - STM32: `HAL_ADC_Start_DMA()`
  - AT32: `at32_adc_dma_transfer_enable()` + `at32_adc_enable()`
  - 寄存器名称也不同：`ADC1->DR` vs `ADC1->adtcdt`
- 以下代码以AT32F4xx HAL为准。
- 置信度：L2 🟡（需TL验证AT32 HAL API名称）

**回应质疑6（遗漏补充）：**

**ADC校准：**
- AT32的ADC上电后需要执行自校准
- 代码：`at32_adc_calibration_init(ADC1); while(at32_adc_calibration_status(ADC1));`
- 不校准会导致±几个LSB的偏差
- 置信度：L2 🟡

**VREF+去耦：**
- VREF+引脚必须接100nF+1μF去耦电容
- 否则参考电压波动直接影响转换精度
- 置信度：L1 🟢（硬件设计基本要求）

**完整信号链路框图：**
```
  LM35 ──► RC低通 ──► (可选:运放) ──► AT32F407 ADC ──► DMA ──► 内存缓冲
  10mV/°C  fc≈10Hz    放大到0~3.3V    12位,0~3.3V   自动搬运   5个采样
                                                                │
                                                                ▼
                                                         中值滤波(去尖峰)
                                                                │
                                                                ▼
                                                         一阶IIR(平滑)
                                                                │
                                                                ▼
                                                         温度值(°C)
                                                                │
                                                                ▼
                                                         应用(显示/控制/上报)
```

**深化后的完整代码（AT32F4xx HAL）：**

```c
/* === ADC + DMA 温度采集（AT32F407） === */

#include "at32f407.h"

#define ADC_BUF_SIZE  5    // 中值滤波窗口
#define IIR_ALPHA     0.1f // IIR平滑系数

static uint16_t adc_dma_buf[ADC_BUF_SIZE];
static volatile uint8_t adc_data_ready = 0;
static float filtered_temp = 25.0f; // 初始值

/* --- 初始化 --- */
void ADC_Temp_Init(void)
{
    // 1. GPIO：PA0 配置为模拟输入
    crm_periph_clock_enable(CRM_GPIOA_PERIPH_CLOCK, TRUE);
    gpio_init_type gpio_init;
    gpio_default_para_init(&gpio_init);
    gpio_init.gpio_pins = GPIO_PINS_0;
    gpio_init.gpio_mode = GPIO_MODE_ANALOG;
    gpio_init(GPIOA, &gpio_init);

    // 2. ADC时钟（不超过14MHz）
    crm_periph_clock_enable(CRM_ADC1_PERIPH_CLOCK, TRUE);
    crm_adc_clock_div_set(CRM_ADC_DIV_6); // 120MHz/6=20MHz... 需要确认

    // 3. ADC配置
    adc_base_config_type adc_config;
    adc_default_para_init(&adc_config);
    adc_config.adc_mode = ADC_MODE_INDEPENDENT;
    adc_config.adc_scan_mode = FALSE;
    adc_config.adc_continuous_mode = TRUE; // 连续转换
    adc_config.adc_data_align = ADC_RIGHT_ALIGNMENT;
    adc_config.adc_ordinary_channel_length = 1;
    adc_base_config(ADC1, &adc_config);

    // 4. 通道配置：PA0 = ADC1_CH0，采样时间239.5周期（最长，精度最高）
    adc_ordinary_channel_set(ADC1, ADC_CHANNEL_0, 1, ADC_SAMPLETIME_239_5);

    // 5. DMA配置
    crm_periph_clock_enable(CRM_DMA1_PERIPH_CLOCK, TRUE);
    dma_init_type dma_init;
    dma_default_para_init(&dma_init);
    dma_init.dma_peripheral_base_addr = (uint32_t)&ADC1->adtcdt;
    dma_init.dma_memory_base_addr = (uint32_t)adc_dma_buf;
    dma_init.dma_direction = DMA_DIR_PERIPHERAL_TO_MEMORY;
    dma_init.dma_buffer_size = ADC_BUF_SIZE;
    dma_init.dma_peripheral_inc = DMA_PERIPHERAL_INC_DISABLE;
    dma_init.dma_memory_inc = DMA_MEMORY_INC_ENABLE;
    dma_init.dma_peripheral_data_width = DMA_PERIPHERAL_DATA_WIDTH_HALFWORD;
    dma_init.dma_memory_data_width = DMA_MEMORY_DATA_WIDTH_HALFWORD;
    dma_init.dma_mode = DMA_MODE_CIRCULAR; // 循环模式
    dma_init.dma_priority = DMA_PRIORITY_HIGH;
    dma_init(DMA1_CHANNEL1, &dma_init);
    dma_channel_enable(DMA1_CHANNEL1, TRUE);

    // 6. ADC DMA使能 + 中断
    adc_dma_transfer_enable(ADC1, TRUE);
    dma_interrupt_enable(DMA1_CHANNEL1, DMA_FDT_INT, TRUE);
    nvic_irq_enable(DMA1_Channel1_IRQn, 5, 0); // 优先级5（中等）

    // 7. ADC校准（必须！）
    adc_calibration_init(ADC1);
    while(adc_calibration_status(ADC1));

    // 8. 使能ADC
    adc_enable(ADC1, TRUE);
}

/* --- DMA完成中断（每5个采样触发一次） --- */
void DMA1_Channel1_IRQHandler(void)
{
    if(dma_flag_get(DMA1_FDT1_FLAG))
    {
        dma_flag_clear(DMA1_FDT1_FLAG);
        adc_data_ready = 1; // 通知主循环处理
    }
}

/* --- 数字滤波：中值 + 一阶IIR --- */
static uint16_t median_filter(uint16_t *buf, uint8_t size)
{
    // 简单排序取中值（5个元素）
    uint16_t tmp[5];
    for(int i = 0; i < size; i++) tmp[i] = buf[i];
    // 冒泡排序（5个元素足够）
    for(int i = 0; i < size-1; i++)
        for(int j = 0; j < size-1-i; j++)
            if(tmp[j] > tmp[j+1]) {
                uint16_t t = tmp[j]; tmp[j] = tmp[j+1]; tmp[j+1] = t;
            }
    return tmp[size/2]; // 中值
}

float ADC_Get_Temperature(void)
{
    if(!adc_data_ready) return filtered_temp;
    adc_data_ready = 0;

    // 1. 中值滤波（去尖峰）
    uint16_t adc_val = median_filter(adc_dma_buf, ADC_BUF_SIZE);

    // 2. ADC值 → 电压
    float voltage = (float)adc_val * 3.3f / 4096.0f;

    // 3. 电压 → 温度（LM35: 10mV/°C）
    float raw_temp = voltage * 100.0f; // °C

    // 4. 一阶IIR平滑
    filtered_temp = IIR_ALPHA * raw_temp + (1.0f - IIR_ALPHA) * filtered_temp;

    return filtered_temp;
}

/* --- 主循环 --- */
int main(void)
{
    system_clock_config(); // 系统时钟配置
    ADC_Temp_Init();

    while(1)
    {
        float temp = ADC_Get_Temperature();
        // 使用温度值：显示、控制、上报...
        // 不需要时可以不读，DMA自动采集不影响主循环
    }
}
```

---

### 步骤2.2 — QA重审+补充

**【角色】QA Engineer（工人，有否决权）**
**【审查模式】灰盒审查（看了SE的代码）**

**审查结果：**

代码审查：
- GPIO配置 → ✅ 模拟输入模式正确
- ADC配置 → ✅ 连续转换、右对齐、单通道
- DMA配置 → ✅ 循环模式、半字宽度、外设到内存
- 校准 → ✅ 有校准步骤
- 中断优先级 → ✅ 设为5，不会抢占关键中断
- 滤波 → ✅ 中值+IIR组合合理

**发现问题：**
- ⚠️ ADC时钟：`crm_adc_clock_div_set(CRM_ADC_DIV_6)`
  如果APB2=120MHz，120/6=20MHz > 14MHz上限！
  应该用DIV_8（120/8=15MHz）或DIV_10。
  **这是一个会导致ADC不工作的Bug！**
- ⚠️ AT32 HAL API名称需要TL验证（`at32_adc_xxx` vs `adc_xxx`）

**补充风险预案：**
- ADC读数跳动大 → 检查RC滤波、VREF+去耦、接地
- DMA数据不更新 → 检查DMA通道是否被其他外设占用
- 温度值偏差大 → 重新校准ADC、检查运放增益
- 系统卡顿 → 检查中断优先级是否太高

**补充回滚方案：**
- 关闭ADC和DMA即可停止采集
- 不影响其他外设（UART、SPI、Timer等）

**【通过】✅**（条件：修正ADC时钟分频、TL验证API名称）

---

### 步骤2.3 — TL亲自验证

**【角色】Tech Lead（干部参加劳动）**

**验证1：ADC时钟分频**
- QA说得对！120MHz/6=20MHz > 14MHz上限。
- 修正：使用 `CRM_ADC_DIV_8`（120/8=15MHz）或确认AT32的ADC时钟上限。
- AT32F407的ADC时钟上限：需要查手册确认是14MHz还是更高。
  AT32F407可能比STM32F4允许更高的ADC时钟。
- 置信度：L3 🟠（需要查AT32F407参考手册确认）

**验证2：AT32 HAL API名称**
- AT32的HAL库API命名与STM32不同。
- 需要确认：`adc_base_config()` 还是 `at32_adc_base_config()`？
- 建议：用户查看 `at32f407_adc.h` 头文件确认API名称。
- 置信度：L3 🟠（API名称可能因HAL版本不同而异）

**验证3：DMA通道分配**
- AT32F407 DMA1 Channel1 → ADC1（与STM32F4相同）
- 确认：如果UART也用了DMA1，需要检查通道是否冲突。
- 置信度：L2 🟡

**TL裁决：**
- SE的方案整体正确，信号链路完整
- QA发现的ADC时钟Bug必须修正
- API名称需要用户根据实际HAL版本确认
- 传感器型号必须向用户确认

---

## 第3轮：定稿

### 最终方案摘要

**【角色】Staff Engineer（技术人员）**

# AT32F407 温度ADC采集方案

## 信号链路
```
LM35 → RC低通(1.5kΩ+10μF,fc≈10Hz) → AT32F407 ADC1_CH0(PA0)
→ DMA1_CH1(循环,5个采样) → 中值滤波 → 一阶IIR → 温度值(°C)
```

## 关键决策
| 决策 | 选择 | 理由 | 排除 |
|---|---|---|---|
| 采集方式 | DMA+中断 | CPU零占用，批量处理 | 轮询（浪费CPU）、纯中断（太频繁） |
| SPI还是DMA | **不是二选一** | SPI是通信协议，DMA是搬运方式 | 内部ADC不需要SPI |
| 滤波 | 中值+IIR | 去尖峰+平滑，内存极小 | 纯均值（延迟大）、FFT（太重） |
| 采样率 | 50Hz | 温度<0.1Hz，留500倍余量 | 1kHz（没必要）、1Hz（余量不够） |
| 模拟前端 | RC无源滤波 | 简单可靠，温度信号不需要有源 | 有源滤波（温度精度不需要） |

## 对系统的影响
| 影响 | 程度 | 对策 |
|---|---|---|
| CPU占用 | <0.1%（DMA搬运） | 无需特殊处理 |
| DMA通道 | 占用DMA1_CH1 | 检查UART/SPI是否冲突 |
| 中断 | 每100ms一次（50Hz×5） | 优先级5，不影响通信 |
| 功耗 | ADC连续运行增加~1mA | 不需要时关闭ADC |
| GPIO | PA0变为模拟输入 | 不能同时做GPIO |

## 待用户确认
- [ ] 传感器型号？（LM35/NTC/其他 → 决定信号调理）
- [ ] 精度要求？（±1°C → 直连；±0.1°C → 运放+基准）
- [ ] 是否有其他外设用DMA？（避免通道冲突）
- [ ] AT32 HAL库版本？（确认API名称）

## 验证清单
- [ ] ADC校准执行成功
- [ ] DMA缓冲区数据在更新
- [ ] 温度读数与实际温度计对比（±2°C内）
- [ ] 主循环不被ADC阻塞
- [ ] 其他外设（UART等）正常工作

---

### QA Sign-off
**【通过】✅** 方案完整，信号链路清晰，系统影响已评估。
待用户确认传感器型号后可进一步细化。

### TL Sign-off
**【同意】✅** 关键裁决：
1. ADC时钟分频必须修正（DIV_8或更高）
2. 传感器型号是方案基础，必须先确认
3. 代码API名称以用户实际HAL版本为准

---

## 讨论统计

| 指标 | 值 |
|---|---|
| 讨论轮次 | 3轮 |
| QA否决 | 1次（6个质疑） |
| SE修正 | 6处 |
| 关键Bug拦截 | 1个（ADC时钟超限） |
| 关键概念澄清 | 1个（SPI vs DMA不是二选一） |
| 遗漏补充 | 4处（校准、去耦、采样率计算、RC参数） |
| 待用户确认 | 4项 |

---

*讨论完成：2026-07-20*
*目标芯片：AT32F407（雅特力，Cortex-M4）*
*治理模型：鞍钢宪法循环讨论（3轮）*
