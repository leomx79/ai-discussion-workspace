# 鞍钢宪法框架回测报告
## 日期：2026-07-20
## 回测对象：鞍钢宪法式AI防幻觉讨论框架（Kilo Code集成版）
## 回测方法：用框架自身的3轮循环讨论方法论，对框架本身进行审查

---

## 第0步：TL系统摸底报告

【角色】Tech Lead（干部）
【模式】系统架构审查
【置信度】L1 🟢 已验证（以下所有结论均基于实际文件检查和CLI验证）

### 验证清单

| # | 验证项 | 方法 | 结果 | 置信度 |
|---|--------|------|------|--------|
| 1 | Agent文件是否被Kilo Code识别 | `kilo agent list` | ✅ 5个Agent全部识别 | L1 |
| 2 | Frontmatter格式是否正确 | 对比内置agent（architect等） | ✅ mode/permission/options格式正确 | L1 |
| 3 | Rules文件是否会被加载 | 搜索kilo.exe二进制中的`.kilo/rules`路径 | ✅ 确认加载`.kilo/rules/*.md` | L1 |
| 4 | Config检查 | `kilo config check` | ✅ "No config warnings" | L1 |
| 5 | API凭证 | `kilo auth list` | ✅ 5个凭证（Kilo/GLM/DeepSeek/Vercel/agent） | L1 |
| 6 | Task工具是否存在 | 搜索kilo.exe中的`"task"`定义 | ✅ 确认存在 | L1 |
| 7 | 子Agent模型配置 | 搜索`subagent_model`字段 | ⚠️ 存在但未配置（继承主Agent模型） | L1 |
| 8 | Agent可用字段 | 提取schema定义 | ✅ 确认18个有效字段 | L1 |
| 9 | Steps限制 | 搜索`steps`字段定义 | ⚠️ 存在，可限制Agent迭代次数 | L1 |
| 10 | 实际运行测试 | 在Kilo Code中发起讨论 | ❌ **未执行**（需要用户手动测试） | L4 |

### 系统概况
- 5个Agent文件：2个协调器（primary）+ 3个角色（all）
- 1个Rules文件：鞍钢宪法治理规则
- 1个AGENTS.md：项目章程
- 全局模型：glm/glm-5.2
- 无subagent_model配置
- 无steps限制配置

---

## 第1轮：发散（问题发现）

---

### 【角色】Tech Lead（干部）
### 【模式】系统架构审查
### 【置信度】见各条标注

## TL发现的系统级问题

### 问题T1：协调器的"3轮×3角色=9次task调用"可能超出steps限制
- **严重程度**：致命
- **证据**：Kilo Code的`steps`字段定义为"Maximum number of agentic iterations before forcing text-only response"。协调器需要至少9次task调用（3轮×3角色）+ 协调文本 = 至少12-15次迭代。如果默认steps限制太低，讨论会在中途被截断。
- **置信度**：L2 🟡（未确认默认steps值，但风险真实存在）
- **影响**：讨论进行到一半被强制停止，输出不完整方案

### 问题T2：子Agent上下文传递是"手动拼接"，不是自动共享
- **严重程度**：严重
- **证据**：task工具调用子Agent时，子Agent获得的是一个**全新的会话**。协调器必须在prompt参数中手动拼接前序讨论的全部内容。这意味着：
  - 第1轮QA收到的prompt = 用户问题 + TL拆解 + SE方案（可能已经很长）
  - 第2轮SE收到的prompt = 用户问题 + 第1轮全部内容 + QA质疑（更长）
  - 第3轮可能超出上下文窗口
- **置信度**：L2 🟡（基于task工具的工作原理推断）
- **影响**：后期轮次上下文溢出，Agent"忘记"前面的讨论

### 问题T3：所有Agent用同一个模型，无法发挥"不同视角"的优势
- **严重程度**：一般
- **证据**：当前全局模型=glm/glm-5.2，无subagent_model配置。所有5个Agent用同一个模型。鞍钢宪法的核心是"不同立场的人看同一个问题"，但同一个模型的"不同角色"本质上还是同一个大脑在自言自语。
- **置信度**：L2 🟡
- **影响**：防幻觉效果打折——同一个模型不太可能质疑自己的知识盲区

### 问题T4：Rules文件是"建议"不是"强制"
- **严重程度**：一般
- **证据**：`.kilo/rules/ansteel-constitution.md`会被加载到Agent的上下文中，但它只是系统提示的一部分。模型可以"忘记"或"忽略"规则。没有代码级的强制执行机制。
- **置信度**：L2 🟡
- **影响**：讨论进行到后期，Agent可能不再标注L1-L4，不再遵守否决权规则

### 问题T5：没有实际运行测试
- **严重程度**：严重
- **证据**：所有验证都是静态的（文件存在、格式正确、CLI识别）。没有在Kilo Code中实际发起一次讨论。
- **置信度**：L1 🟢
- **影响**：不知道实际效果如何

---

### 【角色】Staff Engineer（技术人员）
### 【模式】系统架构分析
### 【置信度】见各条标注

## SE发现的技术问题

### 问题S1：协调器Prompt中的task调用格式可能不正确
- **严重程度**：致命
- **证据**：协调器prompt中写的是`task(agent="tech-lead", prompt="...")`，但这是**伪代码**。实际的task工具调用格式取决于Kilo Code的tool schema。Agent不会"执行"prompt中的伪代码，它需要理解这是"请调用task工具"的指令。
- **置信度**：L2 🟡（Agent应该能理解意图，但格式不明确可能导致调用失败）
- **影响**：协调器可能不知道如何正确调用task工具

### 问题S2：子Agent没有`task`权限，无法嵌套调用
- **严重程度**：一般（设计如此）
- **证据**：tech-lead、staff-engineer、qa-engineer的permission中没有`task: allow`。这意味着子Agent不能再调用其他子Agent。
- **置信度**：L1 🟢（这是有意设计，防止无限嵌套）
- **影响**：无负面影响，但需要确认这是期望行为

### 问题S3：协调器Prompt太长，可能挤占上下文窗口
- **严重程度**：一般
- **证据**：ansteel-orchestrator.md约4.4KB，project-analyzer.md约6.7KB，staff-engineer.md约6.6KB，qa-engineer.md约6.3KB。这些都会作为系统提示占用上下文窗口。加上rules文件1.5KB和AGENTS.md 5.6KB，总系统提示约25KB。
- **置信度**：L1 🟢（文件大小可测量）
- **影响**：对于glm-5.2等模型，25KB系统提示 + 讨论内容可能接近上下文限制

### 问题S4：嵌入式专用知识仍然依赖模型的训练数据
- **严重程度**：严重
- **证据**：staff-engineer.md中有详细的嵌入式检查清单（ADC、SPI、DMA、RTOS等），但这些只是"提醒模型检查什么"。模型对AT32F407的具体寄存器、电气参数的知识仍然来自训练数据，可能过时或错误。框架没有集成数据手册查询能力。
- **置信度**：L2 🟡
- **影响**：AI仍然可能编造AT32F407的寄存器地址或电气参数

### 问题S5：讨论记录没有自动保存机制
- **严重程度**：一般
- **证据**：协调器prompt中写了"保存为Markdown文件"，但这依赖Agent主动调用edit/write工具。如果steps用完或Agent"忘记"，讨论记录就丢失了。
- **置信度**：L2 🟡
- **影响**：讨论结果可能无法追溯

---

### 【角色】QA Engineer（工人，有否决权）
### 【模式】系统架构审查
### 【置信度】见各条标注

## QA发现的问题 + 质疑

### 问题Q1：🔴 否决——"防幻觉"的核心机制是"提示词工程"，不是"制度制衡"
- **严重程度**：致命
- **质疑对象**：整个框架的核心假设
- **问题**：鞍钢宪法的制衡是**人与人之间的制衡**——干部和工人有不同的利益立场、不同的信息来源、不同的判断能力。但AI的"三个角色"是**同一个模型的同一次推理**。tech-lead、staff-engineer、qa-engineer本质上是同一个大脑在扮演三个角色。
  - 同一个模型不知道的东西，三个角色都不知道
  - 同一个模型的推理偏差，三个角色都有
  - "QA否决SE"实际上是"模型自己否决自己"——模型不太可能真正推翻自己刚刚生成的内容
- **风险**：框架给用户一种"经过三方验证"的虚假安全感，实际上幻觉风险并没有本质降低
- **置信度**：L2 🟡（这是AI多角色讨论的固有限制，不是本框架独有的）
- **建议**：
  1. **必须使用不同模型**——至少QA用不同模型（不同训练数据=不同知识盲区）
  2. **必须集成外部验证**——websearch查数据手册、bash执行代码验证
  3. 降低期望：框架的价值在于"结构化讨论流程"和"强制标注不确定性"，不是"消除幻觉"

### 问题Q2：用户的实际使用场景没有被验证
- **严重程度**：严重
- **质疑对象**：框架的实用性
- **问题**：用户是嵌入式开发者，无编程背景。框架设计了5种分析模式、3轮讨论、L1-L4标注……但用户真正需要的是：
  - 打开Kilo Code → 选Agent → 输入"我的ADC读数跳动" → 得到答案
  - 用户不会关心"这是第几轮讨论"、"QA是否否决了"
  - 用户需要的是**结果**，不是**过程**
- **风险**：框架过于复杂，用户用不起来
- **置信度**：L2 🟡
- **建议**：简化用户界面——用户只需要选"方案生成"或"项目分析"，其他全自动

### 问题Q3：glm-5.2模型是否支持task工具调用？
- **严重程度**：严重
- **质疑对象**：技术可行性
- **问题**：task工具需要模型支持function calling / tool use。glm-5.2是否支持？如果模型不支持tool use，整个框架无法工作。
- **风险**：框架在技术上可能无法运行
- **置信度**：L3 🟠（未验证glm-5.2的tool use能力）
- **建议**：立即测试——在Kilo Code中选ansteel-orchestrator，输入一个简单问题，看是否能调用task工具

### 问题Q4：讨论的"实时显示"可能不是用户期望的效果
- **严重程度**：一般
- **质疑对象**：用户体验
- **问题**：用户说"在kilo的调用框里实时显示"。但task工具调用子Agent时，子Agent的响应可能是**折叠的**或**在后台运行**的。用户看到的可能只是协调器的文本输出，而不是三个角色的"对话"。
- **风险**：用户体验不符合期望
- **置信度**：L3 🟠（未实际测试显示效果）
- **建议**：实际测试一次，确认显示效果

### 问题Q5：没有"退出机制"
- **严重程度**：一般
- **问题**：如果讨论陷入死循环（QA反复否决，SE反复修正），协调器没有明确的退出条件。prompt中说"最多3轮"，但如果3轮后QA仍然否决呢？
- **置信度**：L2 🟡
- **建议**：3轮后如果仍有分歧，直接提交用户裁决，不再循环

---

## 第2轮：收敛（深化+修复方案）

---

### 【角色】Staff Engineer（技术人员）
### 【模式】系统架构分析

## 对第1轮问题的修复方案

### 修复F1：解决steps限制问题（对应T1）
**方案**：在协调器agent的frontmatter中添加`steps: 50`
```yaml
---
mode: primary
steps: 50
...
---
```
**理由**：50次迭代足够完成3轮×3角色=9次task调用 + 协调文本 + 文件保存
**置信度**：L2 🟡（需要测试确认steps的实际行为）

### 修复F2：解决上下文传递问题（对应T2）
**方案**：修改协调器prompt，要求每轮讨论后生成**摘要**而不是传递全文
```
第1轮结束后，协调器生成《第1轮摘要》（500字以内），包含：
- TL拆解的子问题
- SE的候选方案（只保留可行的）
- QA的关键质疑
第2轮的task调用只传递《第1轮摘要》+ 第2轮的具体指令
```
**理由**：避免上下文溢出，同时保留关键信息
**置信度**：L2 🟡

### 修复F3：支持不同模型（对应T3、Q1）
**方案**：在kilo.jsonc中配置subagent_model，或在agent frontmatter中指定model
```jsonc
// kilo.jsonc
{
  "subagent_model": "kilo/anthropic/claude-sonnet-latest"
}
```
或者在每个agent的frontmatter中：
```yaml
---
model: kilo/anthropic/claude-sonnet-latest
---
```
**理由**：不同模型有不同知识盲区，QA用不同模型可以真正发现SE的幻觉
**置信度**：L1 🟢（subagent_model字段已确认存在）
**注意**：需要用户有对应模型的API访问权限

### 修复F4：简化task调用指令（对应S1）
**方案**：修改协调器prompt，不用伪代码，改用自然语言指令
```
旧：调用 tech-lead：task(agent="tech-lead", prompt="...")
新：请使用task工具，调用名为"tech-lead"的agent，传递以下指令：...
```
**理由**：自然语言指令比伪代码更容易被模型正确理解和执行
**置信度**：L2 🟡

### 修复F5：添加数据手册验证能力（对应S4）
**方案**：在tech-lead和qa-engineer的prompt中强调：
```
对于芯片特定的技术参数（寄存器地址、电气参数、默认值），
必须使用websearch工具搜索数据手册进行验证。
不要依赖记忆。搜索关键词：[芯片型号] + [参数名] + datasheet
```
**理由**：利用Kilo Code的websearch工具弥补模型知识盲区
**置信度**：L2 🟡（websearch能否找到准确的数据手册信息取决于搜索结果质量）

### 修复F6：自动保存讨论记录（对应S5）
**方案**：在协调器prompt的最后一步强制要求：
```
讨论结束后，你必须使用edit工具将完整讨论记录保存为文件。
文件名：anssteel-讨论记录-[日期]-[主题].md
这是强制要求，不能跳过。即使讨论未完成，也要保存当前进度。
```
**置信度**：L2 🟡

---

### 【角色】QA Engineer（工人，有否决权）
### 【模式】系统架构审查

## QA对修复方案的审查

### 审查F1（steps: 50）：有条件通过
- steps: 50应该足够，但需要测试
- 补充：如果50次用完，协调器应该保存当前进度而不是直接停止
- **条件**：添加"如果接近steps限制，优先保存当前讨论进度"

### 审查F2（摘要传递）：通过
- 摘要策略合理，500字限制可能太紧
- 建议改为"1000字以内"，保留关键技术细节
- 摘要必须包含：所有L3/L4标注的断言（这些是最需要后续验证的）

### 审查F3（不同模型）：通过，但有重要补充
- ⚠️ 用户的Kilo Gateway是oauth认证，不确定是否支持所有模型
- 建议：先测试`kilo/anthropic/claude-sonnet-latest`是否可用
- 如果不可用，退而求其次：用DeepSeek作为QA模型（不同训练数据）
- **关键**：至少QA应该用不同模型，否则"否决权"是假的

### 审查F4（自然语言指令）：通过
- 自然语言比伪代码好
- 但还需要更明确：告诉协调器task工具的参数名是什么
- 建议：在prompt中写"使用task工具，参数agent填'tech-lead'，参数prompt填指令内容"

### 审查F5（数据手册验证）：有条件通过
- websearch是好方向，但搜索结果可能不准确
- 补充：对于AT32系列，Artery的官网数据手册是PDF格式，websearch可能无法直接读取
- 建议：如果用户有数据手册PDF，可以放在项目目录中，用read工具直接读取
- **条件**：在prompt中添加"如果项目目录中有数据手册文件，优先读取本地文件"

### 审查F6（自动保存）：通过
- 强制保存是对的
- 补充：保存位置应该是项目根目录，不是.kilo目录

### QA的额外发现

#### 问题Q6：AGENTS.md和rules文件的内容有重复
- AGENTS.md（5.6KB）和ansteel-constitution.md（1.5KB）有大量重复内容
- 这浪费了上下文窗口
- 建议：AGENTS.md只保留项目概述，详细规则放在rules中

#### 问题Q7：没有"快速模式"
- 用户有时只需要一个简单问题的快速回答
- 3轮讨论对于"AT32F407的ADC有几个通道？"这种问题太重了
- 建议：添加一个"快速咨询"模式——只调用1个Agent，不走3轮流程

---

## 第3轮：定稿

---

### 【角色】Tech Lead（干部）
### 【模式】系统架构审查
### 【置信度】L1 🟢（基于第1-2轮的验证和讨论）

## 三方合议结论

### 框架的核心价值（确认有效）
1. ✅ **结构化讨论流程**：强制"提出→质疑→修正→验证"，比单次问答更有条理
2. ✅ **置信度标注**：L1-L4标签让用户知道哪些结论可信、哪些需要自己验证
3. ✅ **否决权机制**：即使是"自己否决自己"，也比"完全不质疑"好
4. ✅ **嵌入式专用检查清单**：提醒模型检查容易遗漏的点（DMA冲突、优先级反转等）
5. ✅ **Kilo Code集成**：利用现有工具（read/bash/grep/websearch），不需要额外开发

### 框架的核心限制（必须承认）
1. ⚠️ **同一个模型≠真正的多方制衡**：这是AI多角色讨论的固有限制
2. ⚠️ **提示词工程≠代码级强制**：规则可以被模型"忘记"
3. ⚠️ **不能替代人工验证**：最终决策权必须在用户手中

### 问题优先级排序

| 优先级 | 问题 | 修复方案 | 状态 |
|--------|------|----------|------|
| P0 致命 | 未实际运行测试（T5/Q3） | 在Kilo Code中实际测试一次 | 🔴 待用户执行 |
| P0 致命 | task调用格式可能不正确（S1） | 改为自然语言指令（F4） | 🟡 需修改文件 |
| P1 严重 | steps限制可能截断讨论（T1） | 添加steps: 50（F1） | 🟡 需修改文件 |
| P1 严重 | 上下文传递溢出（T2） | 摘要传递策略（F2） | 🟡 需修改文件 |
| P1 严重 | 同一模型=假制衡（T3/Q1） | 配置subagent_model（F3） | 🟡 需配置 |
| P2 一般 | 芯片参数幻觉（S4） | websearch+本地数据手册（F5） | 🟡 需修改文件 |
| P2 一般 | 讨论记录保存（S5） | 强制保存指令（F6） | 🟡 需修改文件 |
| P2 一般 | AGENTS.md与rules重复（Q6） | 精简AGENTS.md | 🟡 需修改文件 |
| P3 建议 | 没有快速模式（Q7） | 添加快速咨询模式 | ⚪ 未来改进 |
| P3 建议 | 没有退出机制（Q5） | 3轮后提交用户裁决 | 🟡 需修改文件 |

### TL裁决
框架的**设计方向正确**，但**尚未经过实际验证**。当前最大的风险不是设计问题，而是"不知道能不能跑起来"。

**立即行动**：
1. 修复P0和P1问题（修改agent文件）
2. 用户在Kilo Code中实际测试一次
3. 根据测试结果迭代改进

**Sign-off**：
- TL：✅ 同意（框架方向正确，需要实际验证）
- SE：✅ 同意（修复方案可行，需要实施）
- QA：⚠️ 有条件同意（必须先完成实际测试，否则不认可"可用"的结论）

---

## 附录：验证证据清单

| 证据 | 来源 | 文件/命令 |
|------|------|-----------|
| 5个Agent被识别 | `kilo agent list` | CLI输出 |
| Rules路径确认 | kilo.exe二进制搜索 | `.kilo/rules` at offset 677320 |
| Config无警告 | `kilo config check` | "No config warnings" |
| 5个API凭证 | `kilo auth list` | Kilo/GLM/DeepSeek/Vercel/agent |
| Task工具存在 | kilo.exe二进制搜索 | `UG8="task"` |
| Agent schema 18字段 | kilo.exe二进制搜索 | `IH8=new Set([...])` |
| subagent_model字段 | kilo.exe二进制搜索 | 确认存在，未配置 |
| steps字段 | kilo.exe二进制搜索 | "Maximum number of agentic iterations" |
| 全局模型glm-5.2 | kilo.jsonc | `"model": "glm/glm-5.2"` |
